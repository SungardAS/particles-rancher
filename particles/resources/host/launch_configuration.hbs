{{#resource "m:core" "spec"
  type="AWS::AutoScaling::LaunchConfiguration"
  BlockDeviceMappings=(
    arrayify
    (
      helper "m:core" "propertySpec"
      type="AWS::AutoScaling::LaunchConfiguration.BlockDeviceMapping"
      DeviceName="/dev/sda1"
      Ebs=(
        helper "m:core" "propertySpec"
        type="AWS::AutoScaling::LaunchConfiguration.BlockDevice"
        DeleteOnTermination="true"
        VolumeSize=(cValue volumeSize)
        VolumeType="gp2"
      )
    )
  )
}}
"Metadata": {
  "AWS::CloudFormation::Init": {{partial "host/cf_init"}}
}
{{/resource}}
